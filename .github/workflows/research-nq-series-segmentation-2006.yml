name: Research NQ Series Segmentation 2006

on:
  push:
    branches: ["research/cagr40-new-alpha-20260901"]
    paths:
      - "scripts/research-nq-series-segmentation-2006.py"
      - ".github/workflows/research-nq-series-segmentation-2006.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  segment:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Extract 2006 filing index
        run: python3 scripts/research-nq-index-extract.py
      - name: Probe N-Q series segmentation
        run: python3 scripts/research-nq-series-segmentation-2006.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: nq-series-segmentation-2006
          path: data/research/nq-series-segmentation-2006.json
          if-no-files-found: warn
