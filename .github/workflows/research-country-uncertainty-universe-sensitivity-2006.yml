name: Research Country Uncertainty Universe Sensitivity 2006
on:
  push:
    branches: ["research/nq-npx-mapping-2006-20260903"]
    paths:
      - "scripts/research-country-uncertainty-universe-sensitivity-2006.py"
      - ".github/workflows/research-country-uncertainty-universe-sensitivity-2006.yml"
permissions:
  actions: read
  contents: read
jobs:
  sensitivity:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Download PIT and final country artifacts
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir -p data/research /tmp/pit /tmp/country
          gh api /repos/kensuke5704/momentum-console/actions/artifacts/9878189715/zip > /tmp/pit.zip
          unzip -q /tmp/pit.zip -d /tmp/pit
          cp /tmp/pit/nq-pit-holdings-2006-ec-filtered.json data/research/
          gh api /repos/kensuke5704/momentum-console/actions/artifacts/9938416132/zip > /tmp/country.zip
          unzip -q /tmp/country.zip -d /tmp/country
          cp /tmp/country/country-final-structural-merge-2006.json data/research/
      - run: python3 scripts/research-country-uncertainty-universe-sensitivity-2006.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: country-uncertainty-universe-sensitivity-2006
          path: data/research/country-uncertainty-universe-sensitivity-2006.json
