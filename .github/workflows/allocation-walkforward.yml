name: Allocation-only anchored walk-forward
on:
  push:
    branches: [research/anchored-walkforward-20260830]
    paths:
      - 'scripts/allocation-walkforward.ts'
      - '.github/workflows/allocation-walkforward.yml'
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx tsx scripts/allocation-walkforward.ts
      - uses: actions/upload-artifact@v4
        with:
          name: allocation-walkforward-result
          path: data/research/allocation-walkforward/result.json