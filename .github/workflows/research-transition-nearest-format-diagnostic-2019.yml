name: Research Transition Nearest Format Diagnostic 2019
on:
  push:
    branches: [research/nq-npx-mapping-2006-20260903]
    paths:
      - scripts/research-transition-nearest-format-diagnostic-2019.py
      - .github/workflows/research-transition-nearest-format-diagnostic-2019.yml
  workflow_dispatch:
jobs:
  diagnose:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/research-transition-nearest-format-diagnostic-2019.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: transition-nearest-format-diagnostic-2019
          path: data/research/transition-nearest-format-diagnostic-2019.json
          if-no-files-found: warn
