name: Recovery bridge exact lifecycle
on:
  push:
    branches: [research/anchored-walkforward-20260830]
    paths:
      - scripts/recovery-bridge-exact-lifecycle.ts
      - .github/workflows/recovery-bridge-exact-lifecycle.yml
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx tsx scripts/recovery-bridge-exact-lifecycle.ts
      - uses: actions/upload-artifact@v4
        with:
          name: recovery-bridge-exact-lifecycle-result
          path: data/research/recovery-bridge-exact-lifecycle/result.json
