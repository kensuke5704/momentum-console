name: Insider raw schema probe
on:
  push:
    branches: [research/anchored-walkforward-20260830]
    paths:
      - 'scripts/insider-raw-schema-probe.py'
      - '.github/workflows/insider-raw-schema-probe.yml'
  workflow_dispatch:
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/insider-raw-schema-probe.py
