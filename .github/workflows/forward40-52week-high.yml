name: Forward40 52-week high diagnostic
on:
  push:
    branches: [research/forward40-alpha-stage1-20260831]
    paths:
      - .github/workflows/forward40-52week-high.yml
      - scripts/forward40-52week-high-diagnostic.ts
  workflow_dispatch:
jobs:
  diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx tsx scripts/forward40-52week-high-diagnostic.ts
      - uses: actions/upload-artifact@v4
        with:
          name: forward40-52week-high-diagnostic
          path: data/research/forward40-52week-high-diagnostic/result.json
