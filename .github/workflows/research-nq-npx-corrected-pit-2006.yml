name: Research NQ N-PX Corrected PIT 2006

on:
  push:
    branches: ["research/nq-npx-mapping-2006-20260903"]
    paths:
      - "scripts/research-nq-npx-mapping-2006.py"
      - "scripts/research-nq-unmapped-diagnostics-2006.py"
      - ".github/workflows/research-nq-npx-corrected-pit-2006.yml"
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  map-corrected:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Download frozen N-PX master and corrected PIT holdings
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir -p data/research /tmp/master /tmp/pit
          gh api /repos/kensuke5704/momentum-console/actions/artifacts/9876020712/zip > /tmp/master.zip
          gh api /repos/kensuke5704/momentum-console/actions/artifacts/9878011119/zip > /tmp/pit.zip
          unzip -q /tmp/master.zip -d /tmp/master
          unzip -q /tmp/pit.zip -d /tmp/pit
          cp /tmp/master/npx-security-master-2006.json data/research/
          cp /tmp/pit/nq-pit-holdings-2006-corrected.json data/research/nq-pit-holdings-2006.json
      - name: Map corrected PIT holdings
        run: python3 scripts/research-nq-npx-mapping-2006.py
      - name: Diagnose remaining unmapped holdings
        run: python3 scripts/research-nq-unmapped-diagnostics-2006.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: nq-npx-corrected-pit-2006
          path: |
            data/research/nq-npx-mapping-2006.json
            data/research/nq-npx-unmapped-diagnostics-2006.json
          if-no-files-found: warn
