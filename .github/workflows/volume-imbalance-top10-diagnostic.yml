name: Volume imbalance Top10 diagnostic
on:
  push:
    branches: [research/anchored-walkforward-20260830]
    paths:
      - scripts/volume-imbalance-top10-diagnostic.ts
      - .github/workflows/volume-imbalance-top10-diagnostic.yml
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx tsx scripts/volume-imbalance-top10-diagnostic.ts
      - uses: actions/upload-artifact@v4
        with:
          name: volume-imbalance-top10-diagnostic-result
          path: data/research/volume-imbalance-top10-diagnostic/result.json
