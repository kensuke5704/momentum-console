name: N-PORT raw holdings diagnostic
on:
  push:
    branches: [research/anchored-walkforward-20260830]
    paths:
      - 'scripts/nport-raw-holdings-diagnostic.ts'
      - '.github/workflows/nport-raw-holdings-diagnostic.yml'
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx tsx scripts/nport-raw-holdings-diagnostic.ts
      - uses: actions/upload-artifact@v4
        with:
          name: nport-raw-holdings-diagnostic-result
          path: data/research/nport-raw-holdings-diagnostic/result.json
