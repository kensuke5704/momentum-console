name: Rolling winner dependence research

on:
  push:
    branches:
      - research/anchored-walkforward-20260830
    paths:
      - 'scripts/rolling-winner-dependence.ts'
      - '.github/workflows/rolling-winner-dependence.yml'
      - 'src/lib/**'
      - 'public/data/market-data.json'
      - 'data/universe-history.json'
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx tsx scripts/rolling-winner-dependence.ts
      - uses: actions/upload-artifact@v4
        with:
          name: rolling-winner-dependence-result
          path: data/research/rolling-winner-dependence/result.json
