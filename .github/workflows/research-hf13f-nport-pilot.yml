name: Research HF 13F NPORT Pilot
on:
  push:
    branches: ["research/cagr40-new-alpha-20260901"]
    paths:
      - "scripts/research-hf13f-nport-pilot.py"
      - "scripts/research-hf13f-nport-pilot-scaled.py"
      - "scripts/research-hf13f-signal-agreement.ts"
      - ".github/workflows/research-hf13f-nport-pilot.yml"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  pilot:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: pip install -q pyarrow
      - run: python3 scripts/research-hf13f-nport-pilot-scaled.py
      - name: Measure production Top2 signal retention
        run: node --import tsx scripts/research-hf13f-signal-agreement.ts
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: hf13f-nport-pilot
          path: data/research/hf13f-nport-pilot.json
          if-no-files-found: warn
