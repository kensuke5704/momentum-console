name: Research NPORT Series Gap Diagnostic 2020

on:
  push:
    branches: ["research/cagr40-new-alpha-20260901"]
    paths:
      - "scripts/research-nport-series-gap-diagnostic-2020.py"
      - "scripts/research-ncsr-nport-overlap-2020.py"
      - "scripts/research-nq-pit-holdings-2006.py"
      - ".github/workflows/research-nport-series-gap-diagnostic-2020.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/research-nport-series-gap-diagnostic-2020.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: nport-series-gap-diagnostic-2020
          path: data/research/nport-series-gap-diagnostic-2020.json
          if-no-files-found: warn
