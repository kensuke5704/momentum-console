name: Hybrid65 boundary stress
on:
  push:
    branches: [research/anchored-walkforward-20260830]
    paths:
      - scripts/hybrid65-boundary-stress.ts
      - .github/workflows/hybrid65-boundary-stress.yml
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx tsx scripts/hybrid65-boundary-stress.ts
      - uses: actions/upload-artifact@v4
        with:
          name: hybrid65-boundary-stress-result
          path: data/research/hybrid65-boundary-stress/result.json
