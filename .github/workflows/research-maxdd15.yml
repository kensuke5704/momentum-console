name: Research CAGR40 Orthogonal Engines

on:
  push:
    branches: ["research/cagr40-orthogonal-20260901"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  screen:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Screen L2 emergency Stage 9
        run: node --import tsx scripts/cagr40-l2-emergency-stage9.ts
