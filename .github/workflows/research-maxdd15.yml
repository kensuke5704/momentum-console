name: Research CAGR40 New Alpha

on:
  push:
    branches: ["research/cagr40-new-alpha-20260901"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  screen:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Screen SBI GLDM+SLV with SH defense
        run: |
          cp scripts/cagr40-sbi-stage18.ts scripts/_sbi-slv-sh.ts
          sed -i 's/IEF/SLV/g' scripts/_sbi-slv-sh.ts
          node --import tsx scripts/_sbi-slv-sh.ts
