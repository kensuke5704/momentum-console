name: Research CAGR40 New Alpha

on:
  push:
    branches: ["research/cagr40-new-alpha-20260901"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  screen:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Screen SBI IEF baseline
        run: node --import tsx scripts/cagr40-sbi-stage18.ts
      - name: Screen SBI SHY substitute
        run: |
          sed 's/"IEF"/"SHY"/g' scripts/cagr40-sbi-stage18.ts > /tmp/sbi-shy.ts
          node --import tsx /tmp/sbi-shy.ts
      - name: Screen SBI TLT substitute
        run: |
          sed 's/"IEF"/"TLT"/g' scripts/cagr40-sbi-stage18.ts > /tmp/sbi-tlt.ts
          node --import tsx /tmp/sbi-tlt.ts
