name: Research SPDR Series Heading Diagnostic 2020

on:
  push:
    branches: ["research/cagr40-new-alpha-20260901"]
    paths:
      - "scripts/research-spdr-series-heading-diagnostic-2020.py"
      - ".github/workflows/research-spdr-series-heading-diagnostic-2020.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Diagnose SPDR filing-time heading/display-name differences
        run: python3 scripts/research-spdr-series-heading-diagnostic-2020.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: spdr-series-heading-diagnostic-2020
          path: data/research/spdr-series-heading-diagnostic-2020.json
          if-no-files-found: warn
