name: Research Independent Alpha

on:
  push:
    branches: ["research/independent-alpha-engine-20260901"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  independent-alpha:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Run TCSR Stage 1
        run: node --import tsx scripts/independent-alpha-tcsr.ts
      - name: Run Stage 2 candidates
        run: node --import tsx scripts/independent-alpha-stage2.ts
      - name: Validate VCB robustness
        run: node --import tsx scripts/independent-alpha-vcb-validation.ts
