name: Top1 concentration extension
on:
  push:
    branches: [research/anchored-walkforward-20260830]
    paths:
      - scripts/top1-concentration-extension.ts
      - .github/workflows/top1-concentration-extension.yml
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx tsx scripts/top1-concentration-extension.ts
      - uses: actions/upload-artifact@v4
        with:
          name: top1-concentration-extension-result
          path: data/research/top1-concentration-extension/result.json
