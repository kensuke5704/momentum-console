name: Fixed60 bridge cash carry tax
on:
  push:
    branches: [research/anchored-walkforward-20260830]
    paths:
      - scripts/fixed60-bridge-cash-carry-tax.ts
      - .github/workflows/fixed60-bridge-cash-carry-tax.yml
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx tsx scripts/fixed60-bridge-cash-carry-tax.ts
      - uses: actions/upload-artifact@v4
        with:
          name: fixed60-bridge-cash-carry-tax-result
          path: data/research/fixed60-bridge-cash-carry-tax/result.json
