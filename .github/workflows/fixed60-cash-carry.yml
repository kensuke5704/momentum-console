name: Fixed60 cash carry diagnostic
on:
  push:
    branches: [research/anchored-walkforward-20260830]
    paths:
      - scripts/fixed60-cash-carry.ts
      - .github/workflows/fixed60-cash-carry.yml
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx tsx scripts/fixed60-cash-carry.ts
      - uses: actions/upload-artifact@v4
        with:
          name: fixed60-cash-carry-result
          path: data/research/fixed60-cash-carry/result.json
