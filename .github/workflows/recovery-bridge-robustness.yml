name: Recovery bridge robustness
on:
  push:
    branches: [research/anchored-walkforward-20260830]
    paths:
      - scripts/recovery-bridge-robustness.ts
      - .github/workflows/recovery-bridge-robustness.yml
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx tsx scripts/recovery-bridge-robustness.ts
      - uses: actions/upload-artifact@v4
        with:
          name: recovery-bridge-robustness-result
          path: data/research/recovery-bridge-robustness/result.json
