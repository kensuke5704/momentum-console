name: Hybrid alpha factor screen
on:
  push:
    branches: [research/anchored-walkforward-20260830]
    paths:
      - 'scripts/hybrid-alpha-factor-screen.ts'
      - '.github/workflows/hybrid-alpha-factor-screen.yml'
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx tsx scripts/hybrid-alpha-factor-screen.ts
      - uses: actions/upload-artifact@v4
        with:
          name: hybrid-alpha-factor-screen-result
          path: data/research/hybrid-alpha-factor-screen/result.json