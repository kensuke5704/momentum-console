# 2006 N-Q / N-CSR ↔ N-PX Mapping — Current Delta

Updated: 2026-09-06 JST  
Branch: `research/nq-npx-mapping-2006-20260903`

This note is a compact technical delta. The canonical continuation document is `docs/research/momentum-handoff-current.md`.

No strategy return, rank, CAGR, MaxDD, Calmar, trade, or broad 2006–2018 performance result was used to select the rules below.

## Hard stop

Do **not** implement the broad historical Universe builder yet.  
Do **not** run broad 2006–2018 Stage21 performance yet.

## Production ordering that historical reconstruction must mirror

Production first filters holdings to EC + US + CORP, then applies source ETF eligibility, then aggregates securities.

Historical analogue must therefore be:
1. source filing/Series selection;
2. raw holdings parse;
3. explicit `COMMON_EQUITY` only;
4. conservative historical US classification;
5. CORP treatment only where evidence supports it;
6. source-name exclusions + 10–120 holdings + total retained weight >=50 + top10 >=25;
7. aggregate with `etfCount >=2 || maxWeight >=4`;
8. Production breadth score and Top80.

Do not run source eligibility on unfiltered raw legacy holdings.

## Old corrected 2006 PIT remains a structural sample, not source-completeness evidence

Active EC-filtered artifact: `9878189715`.

The corrected sample has 20 Series and 936 explicit `COMMON_EQUITY` holdings, but the original source selector used a known-registrant regex. It is therefore valid for parsing/mapping/country diagnostics, not for proving the complete 2006 ETF source population.

## Frozen deterministic N-PX mapping

Frozen N-PX master artifact: `9876020712`.

Old 936-EC sample:
- baseline 654/936 = 69.95% count / 78.98% weight
- deterministic structural 691/936 = 73.90% / 82.49%.

Allowed automatic mapping only:
- exact normalized issuer;
- unique ADR base;
- accepted trailing share-class/jurisdiction/footnote cleanup;
- unique long prefix >=20 when the candidate identity union is exactly one.

No fuzzy/edit-distance auto-accept.

## Country bridge remains accepted for deterministic mapped identities

Final old-sample country artifact: `9944902929`, run `33893804443`.

- US 641 / NON_US 33 / UNKNOWN 262
- all-EC resolved 72.01% count / 80.85% weight
- deterministic mapped holdings resolved 658/691 = 95.22%
- mapped-holding resolved weight 96.14%
- conflict 0.

Accepted hierarchy:
- alphabetic CINS => NON_US
- explicit ADR/GDR => NON_US
- deterministic historical issuer -> historical SEC filing-time state/country evidence
- otherwise UNKNOWN.

No default-US rule.

## Source discovery: old Daily Range statement is superseded

Run `33878671009` was not a successful historical daily-index transport. Artifact inspection showed all tested HTTP Range attempts failed and yielded zero N-Q filings/CIKs.

Official SEC quarterly/H1 inventories are the valid market-wide route:
- Q1 N-Q/N-Q-A: run `33894478362`, artifact `9945162305`, 1,480 filings / 1,285 CIKs
- H1 N-Q/N-Q-A: run `33897359772`, artifact `9946255797`, 3,293 filings / 2,816 CIKs.

## Broad ETF candidate prefilter is not final classification

H1 broad operational prefilter:
- run `33897558123`
- 47 candidates from 2,816 registrants.

Creation Unit + exchange wording anywhere is too permissive because conventional funds discuss investing in ETFs.

Strict issuer-own evidence pilot:
- run `33897878473`
- artifact `9946455266`
- 7/7 controls correct.

Final Series classification must bind ETF operational evidence to the issuer's own Shares/Fund/Series.

## Series/Class regime boundary

Treat **2006-02-06** as the historical Series/Class metadata boundary.

Pre-boundary:
- do not require Series IDs;
- do not use later Series IDs to backfill January;
- use contemporaneously public title/class identity only.

Post-boundary:
- use SEC Series/Class metadata for identity;
- operational ETF evidence may come from an already-public prospectus of the same issuer;
- do not apply trust-global operational evidence to every sibling Series.

## Complete portfolio sources include N-CSR/N-CSRS

N-Q-only reconstruction is superseded.

2005 H2 complete-portfolio inventory:
- 6,968 filings / 2,928 CIKs
- forms observed: N-Q, N-Q/A, N-CSR, N-CSR/A, N-CSRS, N-CSRS/A.

2006 H1 complete-portfolio inventory:
- 6,966 filings / 2,869 CIKs.

For the 47 candidate CIKs:
- H2 2005: 143 filings / 45 CIKs
- H1 2006: 139 filings.

Historical schedule grammar must include `Statement of Net Assets` in addition to the previously accepted portfolio headings.

An amendment supersedes holdings only when the amendment itself contains a complete-portfolio schedule.

## Post-ID source catalog

The older 267-Series catalog is rejected because trust-global/local binding admitted conventional Vanguard sibling funds.

Tightened v3 result:
- artifact `9963958301`
- 198 positive Series
- 333 source occurrences
- conflicts 0
- fetch errors 0
- monthly snapshots: Feb 20 / Mar 172 / Apr 183 / May 192 / Jun 198.

This is the current post-ID artifact.

## Pre-ID source catalog — current artifact

Important defects already fixed/rejected:
- registrant/trust name misread as Series;
- leading `THE` allowing registrant-name exclusion to fail;
- mixed Vanguard trust siblings inheriting ETF evidence without direct class association.

Current direct per-CIK base:
- run `33977213400`
- success
- merged artifact `9972683860`.

Current mixed-trust replacement:
- commit `6f480b4940dae6a0bd69cfa638593265afcd8775`
- run `33977286028`
- success
- final replacement artifact **`9972690542`**.

Use `9972690542` as the next pre-ID input.

Old run `33950337808` is superseded: its latest attempt was cancelled and incomplete.

## Hybrid and holdings status

Bridge pre-ID to post-ID only when:
- same CIK;
- exact normalized Series name;
- uniqueness on both sides.

No fuzzy rename/ticker inference.

The hybrid catalog must be the sole source selector for holdings parsing.

Pre-ID schedules bind via exact contemporaneous legacy title; post-ID schedules bind by Series ID. Missing/ambiguous bindings remain explicit misses.

Previous chained run `33950735883` failed and predates the current repaired pre-ID artifact. It is not a valid holdings result.

## Exact next technical sequence

1. Inspect artifact `9972690542` for absence of conventional Vanguard siblings and registrant-name identities.
2. Merge `9972690542` + post-ID `9963958301` into Jan–Jun hybrid PIT source snapshots.
3. Run hybrid raw holdings extraction from that catalog only.
4. Audit schedule-binding/fetch/parse misses and explicit legacy section attribution.
5. Keep `COMMON_EQUITY` only.
6. Run frozen deterministic N-PX mapping with artifact `9876020712`.
7. Reuse accepted conservative historical country resolver; unresolved stays UNKNOWN.
8. Apply source eligibility only after COMMON_EQUITY + conservative US filtering.
9. Aggregate monthly with Production breadth semantics and Top80.
10. Quantify residual identity/country sensitivity without using ranks or returns to tune rules.
11. Only after aggregate structural reproduction passes state `Universe reconstruction is confirmed`.
12. Then implement the historical builder; still do not immediately open broad 2006–2018 Stage21 performance.
