# CAGR pre-tax label

Production console performance cards display `CAGR（税引前）` so the gross backtest/OOS CAGR is not mistaken for after-tax performance.

No strategy logic, data generation, or performance calculation is changed.
