import {performanceStats,runStrategySimulation} from "../src/lib/backtest";import {PRODUCTION_STRATEGY} from "../src/lib/config";import {fetchYahooHistory} from "../src/lib/yahoo";import {readFile} from "node:fs/promises";import {resolve} from "node:path";import type {EquityPoint,PricePoint,UniverseMonth} from "../src/lib/types";
type MF={histories:Record<string,PricePoint[]>};type UF={history:UniverseMonth[]};const START="2020-01-01",END="2026-08-25";const sleeves={AI:[{u:"QQQ",b:"TQQQ",s:"SQQQ"}],AJ:[{u:"QQQ",b:"TQQQ",s:"SQQQ"},{u:"SMH",b:"SOXL",s:"SOXS"}],AK:[{u:"QQQ",b:"TQQQ",s:"SQQQ"},{u:"SMH",b:"SOXL",s:"SOXS"},{u:"IWM",b:"TNA",s:"TZA"}]} as const;const mean=(x:number[])=>x.reduce((a,b)=>a+b,0)/(x.length||1),med=(x:number[])=>{const a=[...x].sort((p,q)=>p-q),m=Math.floor(a.length/2);return a.length%2?a[m]:(a[m-1]+a[m])/2};
function mret(c:EquityPoint[]){const m=new Map<string,{f:number;l:number}>();for(const x of c){const k=x.date.slice(0,7),v=m.get(k);if(!v)m.set(k,{f:x.equity,l:x.equity});else v.l=x.equity}return new Map([...m].map(([k,v])=>[k,v.l/v.f-1]))}function corr(a:Map<string,number>,b:Map<string,number>){const ks=[...a.keys()].filter(k=>b.has(k)),x=ks.map(k=>a.get(k)!),y=ks.map(k=>b.get(k)!),mx=mean(x),my=mean(y);let n=0,dx=0,dy=0;for(let i=0;i<x.length;i++){const A=x[i]-mx,B=y[i]-my;n+=A*B;dx+=A*A;dy+=B*B}return n/Math.sqrt(dx*dy)}function roll36(c:EquityPoint[]){const a:number[]=[];for(let i=0;i<c.length;i++){const ed=new Date(c[i].date+"T00:00:00Z"),t=new Date(ed);t.setUTCFullYear(t.getUTCFullYear()-3);let j=i;while(j>0&&new Date(c[j].date+"T00:00:00Z")>t)j--;const y=(ed.getTime()-new Date(c[j].date+"T00:00:00Z").getTime())/86400000/365.25;if(y>=2.9)a.push((c[i].equity/c[j].equity)**(1/y)-1)}return a}function trim(c:EquityPoint[],s:string){const z=c.filter(x=>x.date>=s),b=z[0].equity;let p=1;return z.map(x=>{const e=x.equity/b;p=Math.max(p,e);return{date:x.date,equity:e,drawdown:e/p-1}})}
function run(h:Record<string,PricePoint[]>,sl:readonly {u:string;b:string;s:string}[],cost=.001,lag=0){const sy=[...new Set(sl.flatMap(x=>[x.u,x.b,x.s]))],rows=Object.fromEntries(sy.map(s=>[s,[...(h[s]??[])].sort((a,b)=>a.date.localeCompare(b.date)).filter(x=>x.date>=START&&x.date<=END)])),maps=Object.fromEntries(sy.map(s=>[s,new Map((rows[s] as PricePoint[]).map(x=>[x.date,x]))])),idx=Object.fromEntries(sy.map(s=>[s,new Map((rows[s] as PricePoint[]).map((x,i)=>[x.date,i]))]));const dates=(rows[sl[0].u] as PricePoint[]).map(x=>x.date).filter(d=>sy.every(s=>(maps[s] as Map<string,PricePoint>).has(d)));let prev:string[]=[],e=1,p=1;const out:EquityPoint[]=[{date:dates[0],equity:1,drawdown:0}];for(let di=1;di<dates.length;di++){const d=dates[di],pd=dates[di-1],si=Math.max(0,di-1-lag),sd=dates[si],pick:string[]=[];for(const x of sl){const i=(idx[x.u] as Map<string,number>).get(sd)!;if(i<199)continue;const r=rows[x.u] as PricePoint[],on=r[i].close>mean(r.slice(i-199,i+1).map(z=>z.close));pick.push(on?x.b:x.s)}let rr=0;if(pick.length)for(const s of pick){const m=maps[s] as Map<string,PricePoint>;rr+=(m.get(d)!.close/m.get(pd)!.close-1)/pick.length}const all=[...new Set([...prev,...pick])],turn=all.reduce((z,s)=>z+Math.abs((pick.includes(s)?1/Math.max(1,pick.length):0)-(prev.includes(s)?1/Math.max(1,prev.length):0)),0);rr-=.5*turn*cost;prev=pick;e*=1+rr;p=Math.max(p,e);out.push({date:d,equity:e,drawdown:e/p-1})}return out}
async function main(){const m=JSON.parse(await readFile(resolve("public/data/market-data.json"),"utf8"))as MF,u=JSON.parse(await readFile(resolve("data/universe-history.json"),"utf8"))as UF;for(const s of [...new Set(Object.values(sleeves).flatMap(a=>a.flatMap(x=>[x.u,x.b,x.s])))])if(!m.histories[s]||m.histories[s].length<200)m.histories[s]=await fetchYahooHistory(s);const fixed=runStrategySimulation({histories:m.histories,universeHistory:u.history,config:PRODUCTION_STRATEGY}).backtest.equityCurve.filter(x=>x.date>=START&&x.date<=END),fm=mret(fixed),results:any={};for(const [k,sl] of Object.entries(sleeves)){const mk=(c=.001,l=0)=>run(m.histories,sl,c,l),base=mk(),stress=[mk(.003,0),mk(.001,1),mk(.001,2),trim(mk(),"2021-01-01")],sc=stress.map(x=>performanceStats(x).cagr),rw=roll36(base),st=performanceStats(base),proxy=Math.min(med(sc),med(rw)),co=corr(mret(base),fm);results[k]={sleeves:sl.length,stats:st,stressMedianCAGR:med(sc),rolling36MedianCAGR:med(rw),rolling36Worst:Math.min(...rw),planningProxy:proxy,fixed60MonthlyCorrelation:co,pass:proxy>=.25&&st.maxDrawdown>=-.40&&Math.abs(co)<=.45}}console.log(JSON.stringify({architecture:"Daily 200DMA bull/inverse switch on unleveraged underlyings; equal sleeve weights",objective:{planningCAGR:.25,maxDrawdown:-.40,absFixed60Correlation:.45},results},null,2))}main().catch(e=>{console.error(e);process.exitCode=1});