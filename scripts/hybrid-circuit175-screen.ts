import fs from 'node:fs/promises';import path from 'node:path';import {PRODUCTION_STRATEGY} from '../src/lib/config';import {buildMonthlySignal} from '../src/lib/strategy/momentum';import {initialEngineState,transitionDay} from '../src/lib/strategy/state-machine';import {performanceStats} from '../src/lib/backtest';import {nextUsTradingSession} from '../src/lib/trading-calendar';import type {EquityPoint,PricePoint,StrategyConfig,UniverseMonth} from '../src/lib/types';
function config(circuit:number):StrategyConfig{return{...PRODUCTION_STRATEGY,momentum:{...PRODUCTION_STRATEGY.momentum,oneMonth:0,threeMonth:.25,sixMonth:.75},allocation:{...PRODUCTION_STRATEGY.allocation,baseTop1Weight:.6,concentratedTop1Weight:.7},risk:{...PRODUCTION_STRATEGY.risk,portfolioCircuit:circuit}}}
function sim(h:Record<string,PricePoint[]>,u:UniverseMonth[],c:StrategyConfig){const q=[...h.QQQ].sort((a,b)=>a.date.localeCompare(b.date)),ds=q.map(x=>x.date),ix=new Map(ds.map((d,i)=>[d,i])),pm=Object.fromEntries(Object.entries(h).map(([s,p])=>[s,new Map(p.map(x=>[x.date,x]))])),um=new Map(u.map(x=>[x.asOf,x]));let st=initialEngineState(c);const z:EquityPoint[]=[];for(let i=0;i<ds.length;i++){const d=ds[i];if(d<c.backtestStart)continue;const n=ds[i+1]??nextUsTradingSession(d),uu=um.get(d);let sg=uu?buildMonthlySignal({universe:uu,histories:h,qqq:q,nextSessionDate:n,config:c}):null;if(sg&&sg.selectedSymbols.length===2){const w=sg.zGap!==null&&sg.zGap>=.25?.7:.6;sg.targetWeights=[w,1-w]}const ss=new Set(['QQQ',...st.currentPositions.map(p=>p.symbol),...(st.pendingSignal?.selectedSymbols??[]),...st.nextAction.symbols,...(sg?.selectedSymbols??[])]),pr=Object.fromEntries([...ss].map(s=>[s,pm[s]?.get(d)]));st=transitionDay(st,{date:d,prices:pr,qqqHistoryThroughClose:q.slice(0,(ix.get(d)??i)+1),monthlySignal:sg,nextSessionDate:n},c);z.push({date:d,equity:st.currentEquity,drawdown:st.drawdown})}return z}function sl(c:EquityPoint[],a:string,b:string){const x=c.filter(p=>p.date>=a&&p.date<=b);if(!x.length)return[];const q=x[0].equity;return x.map(p=>({...p,equity:p.equity/q}))}
async function main(){const mk=JSON.parse(await fs.readFile(path.join(process.cwd(),'public/data/market-data.json'),'utf8'))as{histories:Record<string,PricePoint[]>},uf=JSON.parse(await fs.readFile(path.join(process.cwd(),'data/universe-history.json'),'utf8'))as{history:UniverseMonth[]},u=[...uf.history].sort((a,b)=>a.asOf.localeCompare(b.asOf)),base=sim(mk.histories,u,config(.15)),cand=sim(mk.histories,u,config(.175)),ys=[2020,2021,2022,2023,2024,2025,2026],annual=ys.map(y=>({y,base:performanceStats(sl(base,`${y}-01-01`,y===2026?'2026-08-25':`${y}-12-31`)),candidate:performanceStats(sl(cand,`${y}-01-01`,y===2026?'2026-08-25':`${y}-12-31`))})),splits=[{tr:'2021-12-31',y:2022},{tr:'2022-12-31',y:2023},{tr:'2023-12-31',y:2024},{tr:'2024-12-31',y:2025},{tr:'2025-12-31',y:2026}],wf=splits.map(s=>{const bt=performanceStats(sl(base,'2020-01-01',s.tr)),ct=performanceStats(sl(cand,'2020-01-01',s.tr)),pick=(ct.calmar??-99)>(bt.calmar??-99)?'CIRCUIT17.5':'CIRCUIT15',e=s.y===2026?'2026-08-25':`${s.y}-12-31`;return{trainThrough:s.tr,oosYear:s.y,pick,oos:performanceStats(sl(pick==='CIRCUIT17.5'?cand:base,`${s.y}-01-01`,e)),base:performanceStats(sl(base,`${s.y}-01-01`,e)),train:{base:bt,candidate:ct}}}),out={generatedAt:new Date().toISOString(),validity:{freeDataOnly:true,noLeverage:true,trueOOS:false,pit:true,nextOpen:true,warning:'Single pre-existing candidate only: W25 Hybrid with portfolio circuit 17.5% instead of 15%. Candidate was motivated by prior independent anchored parameter-transfer; no new threshold grid.'},base:performanceStats(base),candidate:performanceStats(cand),annual,wf};const d=path.join(process.cwd(),'data/research/hybrid-circuit175-screen');await fs.mkdir(d,{recursive:true});await fs.writeFile(path.join(d,'result.json'),JSON.stringify(out,null,2));console.log(JSON.stringify(out,null,2))}main().catch(e=>{console.error(e);process.exit(1)});