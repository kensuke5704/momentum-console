import fs from 'node:fs/promises';import path from 'node:path';import {PRODUCTION_STRATEGY} from '../src/lib/config';import {buildMonthlySignal} from '../src/lib/strategy/momentum';import {initialEngineState,transitionDay} from '../src/lib/strategy/state-machine';import {performanceStats} from '../src/lib/backtest';import {nextUsTradingSession} from '../src/lib/trading-calendar';import type {EquityPoint,PricePoint,UniverseMonth,StrategyConfig} from '../src/lib/types';
function cfg(w3:number,w6:number):StrategyConfig{return{...PRODUCTION_STRATEGY,momentum:{...PRODUCTION_STRATEGY.momentum,oneMonth:0,threeMonth:w3,sixMonth:w6},allocation:{...PRODUCTION_STRATEGY.allocation,baseTop1Weight:.6,concentratedTop1Weight:.7}}}
function sim(h:Record<string,PricePoint[]>,u:UniverseMonth[],c:StrategyConfig){const q=[...h.QQQ].sort((a,b)=>a.date.localeCompare(b.date)),ds=q.map(x=>x.date),ix=new Map(ds.map((d,i)=>[d,i])),pm=Object.fromEntries(Object.entries(h).map(([s,p])=>[s,new Map(p.map(x=>[x.date,x]))])),um=new Map(u.map(x=>[x.asOf,x]));let st=initialEngineState(c);const z:EquityPoint[]=[];for(let i=0;i<ds.length;i++){const d=ds[i];if(d<c.backtestStart)continue;const n=ds[i+1]??nextUsTradingSession(d),uu=um.get(d),sg=uu?buildMonthlySignal({universe:uu,histories:h,qqq:q,nextSessionDate:n,config:c}):null;if(sg&&sg.selectedSymbols.length===2)sg.targetWeights=sg.zGap!==null&&sg.zGap>=.25?[.7,.3]:[.6,.4];const ss=new Set(['QQQ',...st.currentPositions.map(p=>p.symbol),...(st.pendingSignal?.selectedSymbols??[]),...st.nextAction.symbols,...(sg?.selectedSymbols??[])]),pr=Object.fromEntries([...ss].map(s=>[s,pm[s]?.get(d)]));st=transitionDay(st,{date:d,prices:pr,qqqHistoryThroughClose:q.slice(0,(ix.get(d)??i)+1),monthlySignal:sg,nextSessionDate:n},c);z.push({date:d,equity:st.currentEquity,drawdown:st.drawdown})}return z}
function blend(a:EquityPoint[],b:EquityPoint[],wb:number){const bm=new Map(b.map(x=>[x.date,x.equity]));let ea=1,eb=1,pa=a[0]?.equity??1,pb=bm.get(a[0]?.date)??1,peak=1;const z:EquityPoint[]=[];for(const x of a){const y=bm.get(x.date);if(y==null)continue;ea*=x.equity/pa;eb*=y/pb;pa=x.equity;pb=y;const e=(1-wb)*ea+wb*eb;peak=Math.max(peak,e);z.push({date:x.date,equity:e,drawdown:e/peak-1})}return z}
function sl(c:EquityPoint[],s:string,e:string){const x=c.filter(p=>p.date>=s&&p.date<=e);if(!x.length)return[];const b=x[0].equity;return x.map(p=>({...p,equity:p.equity/b}))}
async function main(){const mk=JSON.parse(await fs.readFile(path.join(process.cwd(),'public/data/market-data.json'),'utf8'))as{histories:Record<string,PricePoint[]>},uf=JSON.parse(await fs.readFile(path.join(process.cwd(),'data/universe-history.json'),'utf8'))as{history:UniverseMonth[]},u=[...uf.history].sort((a,b)=>a.asOf.localeCompare(b.asOf)),core=sim(mk.histories,u,cfg(.25,.75)),fast=sim(mk.histories,u,cfg(.5,.5)),rows=[.25,.5].map(w=>{const c=blend(core,fast,w);return{fastWeight:w,full:performanceStats(c),annual:[2020,2021,2022,2023,2024,2025,2026].map(y=>({y,candidate:performanceStats(sl(c,`${y}-01-01`,y===2026?'2026-08-25':`${y}-12-31`)),core:performanceStats(sl(core,`${y}-01-01`,y===2026?'2026-08-25':`${y}-12-31`))}))}}),sp=[{tr:'2021-12-31',y:2022},{tr:'2022-12-31',y:2023},{tr:'2023-12-31',y:2024},{tr:'2024-12-31',y:2025},{tr:'2025-12-31',y:2026}],curves=new Map<number,EquityPoint[]>([[0,core],[.25,blend(core,fast,.25)],[.5,blend(core,fast,.5)]]),wf=sp.map(s=>{const sc=[0,.25,.5].map(w=>({w,train:performanceStats(sl(curves.get(w)!,'2020-01-01',s.tr))})).sort((a,b)=>(b.train.calmar??-99)-(a.train.calmar??-99));const w=sc[0].w,e=s.y===2026?'2026-08-25':`${s.y}-12-31`;return{trainThrough:s.tr,oosYear:s.y,selectedFastWeight:w,oos:performanceStats(sl(curves.get(w)!,`${s.y}-01-01`,e)),core:performanceStats(sl(core,`${s.y}-01-01`,e))}}),o={generatedAt:new Date().toISOString(),validity:{freeDataOnly:true,noLeverage:true,trueOOS:false,warning:'Two-sleeve ensemble only. W25 and W50 each run independently with same risk controls; capital weights 75/25 or 50/50, total max exposure 100%.'},core:performanceStats(core),fast:performanceStats(fast),rows,wf};const d=path.join(process.cwd(),'data/research/horizon-ensemble-screen');await fs.mkdir(d,{recursive:true});await fs.writeFile(path.join(d,'result.json'),JSON.stringify(o,null,2));console.log(JSON.stringify(o,null,2))}main().catch(e=>{console.error(e);process.exit(1)});